#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/C:/Users/tonyny/AppData/Local/Yarn/Data/global/node_modules/.bin/create-umi-app" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/C:/Users/tonyny/AppData/Local/Yarn/Data/global/node_modules/.bin/create-umi-app" "$@"
  ret=$?
fi
exit $ret
